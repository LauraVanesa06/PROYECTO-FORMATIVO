import 'package:flutter/material.dart';

class ListaWidget extends StatelessWidget {
  const ListaWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: [Text('Deposito'), Text('12344'), Text('Total')]);
  }
}
