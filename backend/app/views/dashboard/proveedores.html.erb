<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Dashboard Ferretería</title>
  <%= stylesheet_link_tag "proveedores", media: "all" %>
    <%= stylesheet_link_tag "dashboard", media: "all" %>


  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
  <link href="https://fonts.googleapis.com/css2?family=Eczar:wght@400;500;600;700;800&display=swap" rel="stylesheet">
</head>
<body>

  <header>
    <div class="container-header">
      <div class="img-empresa">
        <img src="img/test.png" alt="" width="60px">
      </div>
      <p class="pricipal">Ferremateriales el Maestro - Dashboard</p>
    </div>
  </header>

  <nav class="nav-bg">
    <div class="container-nav">
      <ul class="menu">
        <li><a href="dashboard">Inicio</a></li>
        <li><a href="inventario">Inventario</a></li>
        <li><a href="ventas">Ventas</a></li>
        <li><a href="proveedores">Proveedores</a></li>
        <li><a href="clientes">Clientes</a></li>
      </ul>
      <div>
          <% if user_signed_in? %>
            <%= button_to "Cerrar sesión", destroy_user_session_path, method: :delete, form: { data: { turbo: true } }, id: "cerrar", class: "logout"%>
          <% end %>
        </div>
    </div>
  </nav>

<section class="section">
<h2 class="text-white bg-dark p-2">Proveedores</h2>

 <%= form_with model: @proveedor, url: crear_proveedor_path, local: true do |form| %>
  <div class="mb-3">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, class: "form-control" %>
  </div>
  <div class="mb-3">
    <%= form.label :direccion %>
    <%= form.text_field :direccion, class: "form-control" %>
  </div>
  <div class="mb-3">
    <%= form.label :tipoProducto %>
    <%= form.text_field :tipoProducto, class: "form-control" %>
  </div>
  <div class="mb-3">
    <%= form.label :telefono %>
    <%= form.text_field :telefono, class: "form-control" %>
  </div>
  <div class="mb-3">
    <%= form.label :correo %>
    <%= form.email_field :correo, class: "form-control" %>
  </div>
  <%= form.submit "Agregar", class: "btn btn-primary" %>
<% end %>

<hr>

<h3>Lista de Proveedores</h3>
<div class="row">
  <% @proveedores.each do |proveedor| %>
    <div class="col-md-4 mb-3">
      <div class="card">
        <div class="card-body">
          <h5 class="card-title"><%= proveedor.nombre %></h5>
          <p class="card-text">
            <strong>direccion:</strong> <%= proveedor.direccion %><br>
            <strong>Producto:</strong> <%= proveedor.tipoProducto %><br>
            <strong>telefono:</strong> <%= proveedor.telefono %><br>
            <strong>Correo:</strong> <%= proveedor.correo %>
          </p>
        </div>
      </div>
    </div>
  <% end %>
</div>
</section>

  <footer>
    &copy; 2025 Ferremateriales el Maestro - Todos los derechos reservados
  </footer>

</body>
</html>