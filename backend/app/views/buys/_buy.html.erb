<div id="<%= dom_id(buy) %>" class="buy-id">
  <div>

      <p>
      <strong>Id de venta:</strong>
      <%= buy.id %>
    </p>
    <p>
  <strong>Tipo de venta:</strong>
  <%= buy.transaction_id.present? ? "Venta en línea (Wompi)" : "Venta física" %>
</p>

<% if buy.transaction_id.present? && buy.payment&.pay_method.present? %>
  <p>
    <strong>Método de pago:</strong>
    <%= buy.payment.pay_method.capitalize %>
  </p>
<% end %>


    <p><strong>Id de venta:</strong> <%= buy.id %></p>
    <p><strong>Nombre del cliente:</strong> <%= buy.customer.nombre %></p>
    <p><strong>Fecha de la compra:</strong> <%= buy.fecha.strftime("%d/%m/%Y") %></p>
    <p>
    <p><strong>Id de venta:</strong> <%= buy.id %></p>
    <p><strong>Nombre del cliente:</strong> <%= buy.customer.nombre %></p>
    <p><strong>Fecha de la compra:</strong> <%= buy.fecha.strftime("%d/%m/%Y") %></p>
    <p>

    <p><strong>Id de venta:</strong> <%= buy.id %></p>
    <p><strong>Nombre del cliente:</strong> <%= buy.customer.nombre %></p>
    <p><strong>Fecha de la compra:</strong> <%= buy.fecha.strftime("%d/%m/%Y") %></p>
    <p>

    <p><strong>Id de venta:</strong> <%= buy.id %></p>
    <p><strong>Nombre del cliente:</strong> <%= buy.customer.nombre %></p>
    <p><strong>Fecha de la compra:</strong> <%= buy.fecha.strftime("%d/%m/%Y") %></p>
    <p>
    <p><strong>Id de venta:</strong> <%= buy.id %></p>
    <p><strong>Nombre del cliente:</strong> <%= buy.customer.nombre %></p>
    <p><strong>Fecha de la compra:</strong> <%= buy.fecha.strftime("%d/%m/%Y") %></p>
    <p>
      <strong>Tipo de venta:</strong>
      <%= buy.payment.present? ? "Venta en línea (Wompi)" : "Venta física" %>
    </p>

    <% if buy.payment.present? %>
      <p><strong>Método de pago:</strong> <%= buy.payment.pay_method %></p>
      <p><strong>Transacción:</strong> <%= buy.payment.transaction_id %></p>
      <p><strong>Monto:</strong> $<%= buy.payment.amount %> <%= buy.payment.currency %></p>
    <% end %>
  </div>
</div>

  <div class="boton-ver">
      <% buy.purchasedetails.each do |p| %>
        <button class="show-buy"
          data-nombre-producto="<%= p.product.nombre %>"
          data-categoria-producto="<%= p.product.category.nombre %>"
          data-proveedor-producto="<%= p.product.supplier.nombre %>"
          data-precio-producto="<%= p.product.precio_cop %>"
          data-nombre-cliente="<%= p.buy.customer.nombre %>"
          data-documento-cliente="<%= p.buy.customer.documento %>"
          data-fecha-compra="<%= p.buy.fecha.strftime("%d/%m/%Y") %>"
          data-tipo-compra="<%= p.buy.tipo %>"
          data-metodo-compra="<%= p.buy.metodo_pago %>"
          data-img-url="<%= p.product.images.attached? ? url_for(p.product.images.first) : '' %>">
          Ver compra
        </button>
      <% end %>
    </div>
</div><div id="<%= dom_id buy %>" class="buy-id">
  <div>
    <p><strong>Id de venta:</strong> <%= buy.id %></p>
    <p><strong>Nombre del cliente:</strong> <%= buy.customer.nombre %></p>
    <p><strong>Fecha de la compra:</strong> <%= buy.fecha.strftime("%d/%m/%Y") %></p>

    <p><strong>Tipo de venta:</strong> <%= buy.payment_id.present? ? "En línea (Wompi)" : "Física" %></p>

    <% if buy.payment_id.present? && buy.payment&.pay_method.present? %>
      <p><strong>Método de pago:</strong> <%= buy.payment.pay_method.capitalize %></p>
    <% end %>
  </div>

  <div class="boton-ver">
    <% buy.purchasedetails.each do |p| %>
      <button class="show-buy"
        data-nombre-producto="<%= p.product.nombre %>"
        data-categoria-producto="<%= p.product.category.nombre %>"
        data-proveedor-producto="<%= p.product.supplier.nombre %>"
        data-precio-producto="<%= p.product.precio_cop %>"
        data-nombre-cliente="<%= p.buy.customer.nombre %>"
        data-documento-cliente="<%= p.buy.customer.documento %>"
        data-fecha-compra="<%= p.buy.fecha.strftime("%d/%m/%Y") %>"
        data-tipo-compra="<%= p.buy.payment_id.present? ? 'En línea (Wompi)' : 'Física' %>"
        data-metodo-compra="<%= p.buy.payment&.pay_method || 'Efectivo' %>"
        data-img-url="<%= p.product.images.attached? ? url_for(p.product.images.first) : '' %>">
        Ver compra
      </button>
    <% end %>
  </div>
</div>
