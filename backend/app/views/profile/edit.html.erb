<!DOCTYPE html>
<html>
<head>
  <title>Editar Perfil - Ferremateriales</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    .edit-profile-container {
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
      font-family: 'Poppins', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    }

    .edit-header {
      background: white;
      border-radius: 20px;
      padding: 30px;
      margin-bottom: 24px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.08);
    }

    .edit-title {
      font-size: 24px;
      font-weight: bold;
      color: #222;
      margin: 0 0 8px 0;
    }

    .edit-subtitle {
      color: #999;
      margin: 0;
    }

    .form-section {
      background: white;
      border-radius: 16px;
      padding: 24px;
      margin-bottom: 16px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.08);
    }

    .form-group {
      margin-bottom: 20px;
    }

    .form-label {
      display: block;
      font-weight: 600;
      color: #333;
      margin-bottom: 8px;
      font-size: 14px;
    }

    .form-input {
      width: 100%;
      padding: 12px 16px;
      border: 2px solid #e8e8e8;
      border-radius: 12px;
      font-size: 15px;
      transition: all 0.2s;
      box-sizing: border-box;
    }

    .form-input:focus {
      outline: none;
      border-color: #2e67a3;
      box-shadow: 0 0 0 3px rgba(46, 103, 163, 0.1);
    }

    .save-button {
      width: 100%;
      padding: 16px;
      background: #2e67a3;
      border: none;
      color: white;
      border-radius: 12px;
      font-weight: 600;
      font-size: 16px;
      cursor: pointer;
      transition: all 0.2s;
      margin-top: 8px;
    }

    .save-button:hover {
      background: #245283;
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(46, 103, 163, 0.3);
    }

    .cancel-button {
      width: 100%;
      padding: 16px;
      background: white;
      border: 2px solid #e8e8e8;
      color: #666;
      border-radius: 12px;
      font-weight: 600;
      font-size: 16px;
      cursor: pointer;
      transition: all 0.2s;
      text-decoration: none;
      display: block;
      text-align: center;
    }

    .cancel-button:hover {
      border-color: #ccc;
      background: #f8f9fa;
    }

    .back-button {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 12px 20px;
      background: white;
      border: 2px solid #2e67a3;
      color: #2e67a3;
      border-radius: 12px;
      text-decoration: none;
      font-weight: 600;
      margin-bottom: 20px;
      transition: all 0.2s;
    }

    .back-button:hover {
      background: #2e67a3;
      color: white;
    }

    .avatar-section {
      text-align: center;
      margin-bottom: 24px;
    }

    .avatar-preview {
      width: 100px;
      height: 100px;
      border-radius: 50%;
      border: 3px solid #2e67a3;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      background: rgba(46, 103, 163, 0.1);
      margin-bottom: 16px;
    }

    .avatar-preview svg {
      width: 50px;
      height: 50px;
      fill: #2e67a3;
    }

    .change-photo-button {
      display: inline-block;
      padding: 8px 20px;
      background: rgba(46, 103, 163, 0.1);
      color: #2e67a3;
      border-radius: 8px;
      font-size: 14px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.2s;
    }

    .change-photo-button:hover {
      background: #2e67a3;
      color: white;
    }

    @media (max-width: 768px) {
      .edit-profile-container {
        padding: 16px;
      }

      .edit-header {
        padding: 20px;
      }

      .form-section {
        padding: 20px;
      }
    }
  </style>
</head>
<body style="background: #f5f5f5; margin: 0; padding: 20px 0;">
  
  <div class="edit-profile-container">
    <%= link_to profile_path, class: 'back-button' do %>
      <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <path d="M19 12H5M12 19l-7-7 7-7"/>
      </svg>
      Volver al perfil
    <% end %>

    <div class="edit-header">
      <h1 class="edit-title">Editar Perfil</h1>
      <p class="edit-subtitle">Actualiza tu información personal</p>
    </div>

    <%= form_with(model: @user, url: profile_path, method: :patch, local: true) do |f| %>
      <div class="form-section">
        <div class="avatar-section">
          <div class="avatar-preview">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor">
              <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm0 3c1.66 0 3 1.34 3 3s-1.34 3-3 3-3-1.34-3-3 1.34-3 3-3zm0 14.2c-2.5 0-4.71-1.28-6-3.22.03-1.99 4-3.08 6-3.08 1.99 0 5.97 1.09 6 3.08-1.29 1.94-3.5 3.22-6 3.22z"/>
            </svg>
          </div>
          <label for="avatar-upload" class="change-photo-button">
            Cambiar foto
          </label>
          <%= f.file_field :avatar, id: 'avatar-upload', style: 'display: none;', accept: 'image/*' %>
        </div>

        <div class="form-group">
          <%= f.label :name, "Nombre completo", class: 'form-label' %>
          <%= f.text_field :name, class: 'form-input', placeholder: 'Ingresa tu nombre completo' %>
        </div>

        <div class="form-group">
          <%= f.label :email, "Correo electrónico", class: 'form-label' %>
          <%= f.email_field :email, class: 'form-input', placeholder: 'tu@email.com' %>
        </div>
      </div>

      <div class="form-section">
        <%= f.submit "Guardar cambios", class: 'save-button' %>
      </div>

      <%= link_to "Cancelar", profile_path, class: 'cancel-button' %>
    <% end %>

  </div>

  <script>
    // Preview de imagen cuando se selecciona un archivo
    document.getElementById('avatar-upload').addEventListener('change', function(e) {
      const file = e.target.files[0];
      if (file) {
        const reader = new FileReader();
        reader.onload = function(event) {
          const preview = document.querySelector('.avatar-preview');
          preview.innerHTML = `<img src="${event.target.result}" style="width: 100%; height: 100%; object-fit: cover; border-radius: 50%;">`;
        };
        reader.readAsDataURL(file);
      }
    });
  </script>

</body>
</html>
