<!DOCTYPE html>
<html>
  <head>
    <title>
      <%= content_for(:title) || "Appsena" %>
    </title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>
    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :abootstrap, "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag :category, "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag :application, "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag :login, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
    <!-- Bootstrap CSS CDN -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
<!-- Bootstrap JS (opcional, para dropdowns/modals) -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  </head>

  <body  class="bg-light">
    <main>
      <header class="custom-header">

        <div class="header-icon"><%= image_tag("header-icon.png", alt: "llave", class: "h-icon" ) %></div>
        <div class="header-text">
          <div class="text1-header">ERREMATERIALES</div>
          <div class="text2-header">EL MAESTRO</div>
        </div>
        <div class="header-icon2"><%= image_tag("header-icon2.png", alt: "llave", class: "h-icon2" ) %></div>
      </header>
      
      <nav class="d-flex justify-content-between align-items-center px-3 py4">
        <div>
          <%= link_to "Inicio", authenticated_root_path, class: "text-danger me-4" %>
          <%= link_to 'Productos', productos_path, class: "text-danger me-4" %>
          <%= link_to 'Contactos', contactos_path, class: "text-danger" %>
        </div>
        <div>
          <% if user_signed_in? %>
            Bienvenido, <%= current_user.email %> |
            <%= button_to "Cerrar sesión", destroy_user_session_path, method: :delete, form: { data: { turbo: true } }, id: "cerrar", class: "btn btn-outline-danger btn-sm" %>
          <% else %>
            <%= link_to new_user_session_path do %>
              <%= image_tag("nav-icon.png", alt: "Iniciar sesión", class: "login-icon") %>
            <% end %>
          <% end %>
        </div>
      </nav>
      <section id="main">
       
        <section id="content">
            <%= yield %>         
        </section>
      </section>
      
      <footer class="bg-dark text-white text-center p-3 mt-auto" id="footer">
        &copy; 2025 Ferremateriales. 3456674556y.
      </footer>

    </main>
  </body>
</html>
