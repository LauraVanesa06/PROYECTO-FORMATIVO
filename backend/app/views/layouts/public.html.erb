<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Tienda Pública</title>

  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <!-- Bootstrap y FontAwesome -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

  <!-- Estilos personalizados -->
  <%= stylesheet_link_tag 'abootstrap', media: 'all' %>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbo-track': 'reload' %>
</head>

<body>
  <header class="bg-light py-3 border-bottom mb-4">
    <div class="container d-flex justify-content-between align-items-center">
      <div class="d-flex align-items-center">
        <span class="ms-3 fs-4">Mi Tienda</span>
      </div>
      <nav>
        <%= link_to "Inicio", home_index_path %> |
        <%= link_to "Productos", productos_path %> |
        <%= link_to "Contáctanos", contactos_path %>
      </nav>
      <div>
        <% if user_signed_in? %>
          <%= link_to "Cerrar sesión", destroy_user_session_path, method: :delete %>
        <% else %>
          <%= link_to "Iniciar sesión", new_user_session_path %> |
          <%= link_to "Registrarse", new_user_registration_path %>
        <% end %>
      </div>
    </div>
  </header>

  <section id="main">
    <div id="content" class="container">
      <%= yield %>
    </div>
  </section>

  <footer class="bg-dark text-light text-center py-3 mt-4">
    &copy; <%= Time.now.year %> Mi Tienda. Todos los derechos reservados.
  </footer>

  <!-- Scripts -->
  <%= javascript_include_tag 'application2' %>
  <%= javascript_include_tag 'mi_script' %>
</body>
</html>
